# Template for the one-time user-provisioning script (scripts/private/provision_users.mjs).
# Copy to scripts/private/.env.service.local and fill in real values there —
# that whole folder is gitignored, this template is just documentation.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
