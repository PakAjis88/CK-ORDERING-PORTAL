# Copy to .env.local for local dev (never commit .env.local — it's gitignored via *.local).
# In production these are set as Netlify environment variables, not committed anywhere.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
